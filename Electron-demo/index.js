const electron = require('electron')
// const {app} = require ('electron');
// app.on('window-all-closed', () => {
//   app.quit()
// })
console.log (1111,electron);
app.on ('ready', () => {
  console.log (1111);
  // app.quit()
});
